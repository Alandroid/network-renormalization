# Actual project dependencies. The previous file was a pip freeze of an
# unrelated machine environment (Pillow, torch, nltk, OpenCV, ROS, Ubuntu
# system packages, a local Mercator path, etc.).
#
# Mercator is a separate install:
#   pip install git+https://github.com/networkgeometry/mercator.git

matplotlib==3.10.1
networkx==3.4.2
numpy==1.26.4
pandas==2.2.3
python-louvain==0.16
